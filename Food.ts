class Food{

  constructor(x,y)
    x = currentPosition;
    y = currentPosition;


  eat()
    isCurrentlyActive = false;

  get position()
    return currentPosition;

  get isActive()
    return isCurrentlyActive;

  get type()
    return food;

implements Actor {


  }
}