import Player from "./Player";

class HumanPlayer{
  
  constructor(SnakeController, InputHandler){
    SnakeController = null;
    InputHandler = null;
  }

  makeTurn(){
    if InputHandler.makeLeftMove = true;
      return SnakeController.turnSnakeLeft;

    if InputHandler.makeRightMove = true;
      return SnakeController.turnSnakeRight;
  }

}